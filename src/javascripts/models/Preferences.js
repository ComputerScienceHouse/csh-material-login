export default class Preferences {
    constructor(options) {
        this.background = options.background;
        this.customBackgroundUrl = options.customBackgroundUrl;
        this.particles = options.particles;
        this.particlesConfig = options.particlesConfig;
    }
}